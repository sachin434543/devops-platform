apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devops-platform.fullname" . }}-test-connection"
  labels:
    {{- include "devops-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devops-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
